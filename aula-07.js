// Declara um array chamado 'idades' contendo três elementos numéricos
const idades = [18, 14, 53]; // 0=18, 1=14, 2=53

// Loop que itera sobre cada elemento do array 'idades'
for (let i = 0; i < idades.length; i++) {
    // Imprime o elemento atual do array 'idades' no console
    console.log('resultado', idades[i]);
}


