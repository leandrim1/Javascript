// Inicializa a variável 'idade' com o valor 1
let idade = 1;

// Inicia um loop 'do...while' que executa pelo menos uma vez
do {
    // Imprime 'PASSOU' seguido do valor atual de 'idade'
    console.log('PASSOU', idade);
    // Incrementa 'idade' em 1
    idade = idade + 1;
} while (idade < 18); // Continua o loop enquanto 'idade' for menor que 18
